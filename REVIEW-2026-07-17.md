# plot3d.xyz 问题排查报告

> 排查日期：2026-07-17（2026-07-18 补充完成剩余检查项，见 §六）
> 排查方式：`tsc` / `eslint` / `vitest` 客观检查 + 关键代码人工核查 + esbuild 打包实测（与 scipy 参考值对比）
> 对照文档：`REVIEW-2026-06-21.md`（旧评审）、`AGENTS.md`

---

## 一、排查计划与执行状态

| 计划项 | 状态 | 结果摘要 |
|--------|------|---------|
| 客观检查（tsc / eslint / vitest / npm audit） | ✅ 完成 | tsc 通过；eslint 11 error + 4 warning；vitest 438 过 / 1 失败；audit 因 registry mirror 不可用 |
| 算法与数值正确性（src/utils 各统计/拟合模块） | ✅ 完成（07-18 补齐） | 新发现 8 个确定性 bug，含 LM 拟合 Jacobian 符号取反、detectPeaks 只检出最高峰等，见 §六.1 |
| 安全（CSP、LaTeX 清洗、xlsx、项目文件校验） | ✅ 完成 | 3 项确认问题，见下文 |
| 组件与状态（worker 竞态、DataTable 历史、虚拟化） | ✅ 完成 | worker 无竞态；DataTable 按键历史与虚拟化均已修复（但"失焦提交历史"实现是坏的，见 §六.3） |
| i18n 一致性 | ✅ 完成 | zh/en 键集合对等（682=682），但 24 个代码在用键缺失 |
| 死代码 / 未使用导出全量扫描 | ✅ 完成（07-18） | 11 处确定未使用导出 + 2 个纯死文件 + 一批"算法已实现未接 UI"的孤儿功能，见 §六.2 |
| historyStore 撤销/重做边界核查 | ✅ 完成（07-18） | 4 个确定 bug：单元格编辑实际不可撤销、restoreBranch 后 redo 颠倒等，见 §六.3 |
| 导出路径资源清理（revokeObjectURL 等）核查 | ✅ 完成（07-18） | CSV 导出泄漏 blob URL；3D 离屏导出从不 purge（WebGL context 耗尽风险），见 §六.4 |

---

## 二、客观检查结果

- `npm run check`（tsc -b --noEmit）：**通过**。
- `npm run lint`：**11 error + 4 warning**，见 §三.4。
- `npm test`（vitest）：25 个测试文件，**438 通过 / 1 失败**。
- `npm audit`：**不可用** —— 当前 npm registry 指向 npmmirror，该镜像未实现 audit 端点，安全审计事实上无法执行。

---

## 三、已确认问题

### 1. 确定性 Bug（P0）

**1.1 `shapiroWilk` 正态性检验对任意输入返回 NaN**

- 位置：`src/utils/hypothesisTests.ts:452-459`
- 现象：用 esbuild 打包后实测 n = 3/4/5/6/10/50，返回的 W 与 p **全部为 NaN**。
- 根因：Royston 近似中 `eps` 计算结果为负，`Math.sqrt(eps)` 产生 NaN 并污染后续全部计算。`AGENTS.md` 的 Tier A 表标注"Phase 1 修复"，但实际未修。
- 影响：统计页的正态性检验功能完全不可用，且无任何测试覆盖该函数（`hypothesisTests.test.ts` 存在但未测到此路径，测试全绿属假阳性）。

**1.2 测试硬编码开发者本机路径，默认测试套件必失败**

- 位置：`tests/realXlsxDate.test.ts:15`
- 现象：读取 `/home/fwt/Downloads/request-usage-2026-07-07.xlsx`，文件不存在时 `npm test` 必然 1 个用例失败（ENOENT）。
- 影响：CI/其他机器上测试永远红灯；真实 bug 会被这个常驻失败掩盖。应把 fixture 提交进仓库或跳过。

### 2. 安全（P1）

**2.1 CSP 形同虚设**

- 位置：`index.html:7`
- 现象：`script-src 'self' 'unsafe-inline' 'unsafe-eval'`，`default-src` 同时放行 `data: blob:`。允许内联脚本 + eval 后，CSP 对 XSS 的防护基本失效。
- 建议：梳理出真正需要 inline/eval 的依赖（主题预置脚本可改用 hash/nonce），逐步收紧；至少把 `default-src` 中的宽松项收敛到具体指令。

**2.2 LaTeX 渲染失败回退未转义 —— 潜在 XSS**

- 位置：`src/utils/latex.ts:34`
- 现象：`renderLatexToHTML` 的 `catch` 分支返回 `<span style="color:#f87171">${latex}</span>`，**用户原始输入未做 HTML 转义**。该返回值经 `dangerouslySetInnerHTML` 注入（`AnnotationRenderer.tsx:423`、`AnnotationOverlay.tsx:155`）。`throwOnError:false` 下 KaTeX 正常不抛异常，但深嵌套等输入可触发 RangeError 走入 catch，此时若内容含 `<img onerror=...>` 即被注入。
- 建议：catch 分支改用 `escapeHtml(latex)`（本文件已有该函数）。

**2.3 xlsx 0.18.5 为带已知漏洞的官方 npm 版**

- 位置：`package.json:32`；`package-lock.json` 中 `resolved` 为 `registry.npmmirror.com/xlsx/-/xlsx-0.18.5.tgz`
- 事实：官方 npm 的 0.18.5 存在 CVE-2023-30533（原型污染）与 CVE-2024-22363（ReDoS），SheetJS 的修复版只发布在其自有 CDN（0.20.x），npm registry 不会更新。
- 附带问题：`AGENTS.md` §8 声称"使用 SheetJS 官方 CDN 版本"，与 lock 文件不符。
- 影响面：应用解析用户上传的 Excel，正是漏洞触发路径。

**2.4 24 个代码在用 i18n 键在两个语言包中均缺失**

- 证据：`grep` 全量 `t('...')` 提取 630 个键，其中 24 个在 `zh.json`/`en.json` 中均不存在。
- 分级：
  - 无 `defaultValue` 兜底、**直接显示原始键名**：`data.usedInActiveChart`（`DataTable.tsx:483`，列标题 tooltip）。
  - 有 `defaultValue` 兜底（中文界面显示英文）：`export.cancel/close/dimensions/...`、`findReplace.*`、`chart2d.loading`、`fit.pinEquation`、`toast.annotationAdded`、`history.setGap`、`history.setGrid` 等 23 个。
- 建议：补齐两语言键；可在 CI 加"键必须存在于 zh/en"的校验脚本。

### 3. 工程卫生（P2）

**3.1 eslint 11 error（`npm run lint` 红）**

| 位置 | 问题 |
|------|------|
| `src/store/chartStore.ts:366` | `let subplots` 从未重新赋值，应为 `const`（prefer-const） |
| `src/utils/curveFitting.ts:1833` | 逗号表达式 `bestSSE = sse, bestParams = [...params]`（no-unused-expressions） |
| `src/utils/curveFitting.ts:1874` | `counter` 变量自增后从未使用（死代码） |
| `src/utils/curveFitting.ts:1092,1097` | 两条 unused eslint-disable 指令 |
| `src/utils/shareLink.test.ts` ×4 | 测试中使用 `any` |
| `tests/svgFigureViewBox.test.ts` ×4 | 声明后未使用的变量 |

另有 4 个 warning：`AnnotationCanvas.tsx:314`、`Workspace.tsx:179` 的 hooks 依赖缺失等。

**3.2 `curveFitting.ts` 全局拟合段存在明显死代码**

- 位置：`src/utils/curveFitting.ts:1815-1888`（`lmFitGlobal`）
- 现象：`counter` 三处"重置+自增"但从不读取；`void predict` 注释自称"kept for API parity"。是生成/重构残留，降低可读性。

**3.3 npm audit 失效**

- 见 §二。registry 切回官方源或单独跑 `npm audit --registry=https://registry.npmjs.org` 才能恢复安全审计能力。

### 4. 文档漂移（AGENTS.md 与现状不符，P2）

| AGENTS.md 声称 | 实际现状 |
|----------------|---------|
| §7"当前项目没有配置测试框架，也没有 .test.* 文件" | 已有 vitest + 25 个测试文件、438 个用例 |
| 大文件表：`ChartView.tsx` 924 行待拆分 | 已拆分，现仅 114 行 |
| Tier A 表：`curveFitting.ts` 1048 行 | 现 1888 行 |
| §5.7 / §一：`PROJECT_VERSION` 为 `2` | 现为 `6`（已有 projectFileV6.test.ts） |
| §9：DataTable "尚未引入虚拟滚动" | 已实现（`DataTable.tsx:261`，>200 行启用） |
| §8：xlsx "使用 SheetJS 官方 CDN 版本" | lock 文件为 npmmirror 的 0.18.5（带 CVE） |
| Tier A 表：shapiroWilk "Phase 1 修复" | 未修复，见 §三.1.1 |

文档漂移本身会误导后续开发与 agent，建议修复代码问题后同步重写这几节。

---

## 四、旧评审（2026-06-21）问题的修复状态抽查

已确认修复：AdSense 移除、依赖版本号可安装、默认色图 viridis（`chartStore.ts:47`）、DataTable 失焦才记历史（`DataTable.tsx:580-581`）、DataTable 虚拟化、主题/语言 localStorage 持久化 + 启动 splash、Tailwind darkMode 与 `[data-theme]` 对齐、CSP 已添加（但过宽，见 §三.2.1）、ErrorBoundary 已挂载、Worker 拟合已实现且按 id 匹配无竞态。

仍需确认/疑似未修（2026-07-18 已全部核实，见 §六.5）：

- **EPS 导出**：已确认从所有 UI 入口移除，仅剩 `exportEps` 孤儿 i18n key。
- **PDF 矢量导出**：已确认仍为光栅化（PNG 嵌入），且 `FileTab.tsx:284` 注释"vector image"名不符实。
- **拟合统计完整性**：UI 已呈现方程/R²/调整 R²/参数表/CI/F 检验/残差图；置信带、加权、bounds、globalFit、6 种新 LM 拟合类型仍未接 UI。

---

## 五、建议处理顺序

1. 修 `shapiroWilk`（含补测试）与 `realXlsxDate.test.ts` 硬编码路径 —— 让测试套件恢复可信。
2. `latex.ts` catch 分支转义 —— 一行修复，消除潜在 XSS。
3. 清 eslint 11 error，让 `npm run lint` 变绿并纳入 CI。
4. 补齐 24 个 i18n 缺失键（优先无兜底的 `data.usedInActiveChart`）。
5. 评估 xlsx 升级路径（SheetJS CDN 0.20.x 或替换为 ExcelJS 等）。
6. 收紧 CSP（至少去掉 `script-src` 的 `unsafe-eval`，给内联主题脚本加 hash）。
7. 同步重写 AGENTS.md 漂移段落。

**2026-07-18 补充后，以下项应插入到最高优先级**（均为静默错误，用户无感知）：

- §六.1 的 LM 拟合 Jacobian 符号 bug（6 种拟合 + globalFit 静默返回初值）与 `detectPeaks` prominence bug（多峰只检出一个）。
- §六.3 的单元格编辑不可撤销 bug（核心交互承诺失效）。

---

## 六、2026-07-18 补充检查结果

> 排查方式：TypeScript 编译器 API 全量引用分析 + esbuild 打包实测（参考值由 scipy 生成）+ 逐调用点代码核查。

### 6.1 算法与数值正确性深查（对应 §一"算法"项）

**`curveFitting.ts` —— 3 个确定性 bug + 2 个发散问题：**

1. **BUG（P0）：`lmFitGeneral` Jacobian 符号取反，所有 LM 拟合静默返回初值**。`curveFitting.ts:1249` 把模型导数取负成了残差导数，更新步恒指向 SSE 增大方向，全部被拒绝，最终返回初值。影响 `lorentzianFit`、`weibullFit`、`logistic4PLFit`、`logistic5PLFit`、`hillFit`、`biexponentialFit` 全部。复现：`y=2·e^(−0.5x)` 精确数据 + 初值 `[1.5,0.3]` → 原样返回初值（期望 `[2,0.5]`）；翻转符号后精确收敛。
2. **BUG（P0）：`lmFitGlobal` 同样符号错误**（`curveFitting.ts:1848`）→ `globalFit` 连线性模型都拟合不了（两个数据集 `y=2x+1`/`y=2x+5` 返回初值，r2=−0.85）。
3. **BUG（P2）：`computePredictionBand` 多项式分支 SE 公式矩阵未转置**（`curveFitting.ts:1671-1676`），degree=1 时与 linear 分支结果都不一致。
4. **问题（P2）：`logisticFit` 无阻尼 Gauss-Newton 发散**（`curveFitting.ts:889-1013`），干净 sigmoid 数据 k=0.5/2 时返回 r2 为负的垃圾结果且无失败信号。
5. **问题（P2）：`gaussianFit` 在 n=3（允许的最小样本）时发散**，返回 r2=−61.75；建议 n<4 返回 null 或加阻尼。

**`peakDetection.ts` —— 1 个确定性 bug：**

6. **BUG（P0）：`detectPeaks` prominence 在邻侧有更高峰时归零**（`peakDetection.ts:54,60`），遇到更高峰时把 base 重置为峰自身高度 → prominence=0 → 该峰被丢弃。**净效果：多峰数据只检出全局最高峰**。级联影响：`multiPeakFit` 自动寻峰路径只拟合一个峰。修复方向：删掉两处 base 重置（min-so-far 已是正确 saddle）。

**`dataProcessing.ts` —— 2 个确定性 bug：**

7. **BUG（P1）：`cubicSplineInterp` 二阶导数差 2 倍**（`dataProcessing.ts:289-307`），三对角解出的是系数 c=M/2 但公式按 M 使用；线性数据上误差为零故测试覆盖不到。修正后与 scipy 逐位一致。
8. **BUG（P2）：`savitzkyGolay` 边界截断窗口未归一化**（`dataProcessing.ts:88-96`），恒定数据边缘输出 0.743/1.086（应为 1）；第 94 行算了 `weight` 却只用于判零、未用于归一化，显然是写漏。

**`statistics.ts` —— 2 个公式错误：**

9. **BUG（P1）：`skewness` 分母误用样本标准差**（`statistics.ts:130,138`），调整 Fisher-Pearson 偏度要求总体二阶矩 m2；结果系统性偏小 ((n−1)/n)^1.5 倍。
10. **BUG（P1）：`kurtosis` 同样问题**（`statistics.ts:149,158`）；级联影响 `jarqueBeraTest`（修好偏度峰度即自愈）。

**抽查通过的模块**：`distributions.ts` 全部实测匹配 scipy（normalCdf/tCdf/chi2/fCdf/betainc/gammainc 等，误差 ≤1e-6）；`multiPeakFit.ts` 拟合器本体（人工初峰双峰精确还原、洛伦兹面积公式正确）；`statistics.ts` 的 mean/variance/quantile/pearson/spearman/kendall/durbinWatson/breuschPagan/VIF 等；`dataProcessing.ts` 的 movingAverage/whittaker/linearInterp/fillMissingValues/outliers 等。

**参考差异（非 bug）**：`akimaInterp` 端点斜率公式与 scipy 不同（内部区间一致）；`pchipInterp` 端点导数用割线斜率（单调性保持）；`peakTypes.ts` 注释称 linear 背景为 `[intercept, slope]`，实际为 `[slope, intercept]`。

**共性根因**：这批 bug 全在"完美数据即通过"的测试盲区——现有测试多用精确数据，初值即答案（LM bug）、修正项为零（样条 bug）、只断言"返回非 null"（发散问题）。建议为每个 bug 补"扰动数据 + 参考值对比"的回归测试。

### 6.2 死代码 / 未使用导出全量扫描（对应 §一"死代码"项）

方法：tsc 编译器 API 对 src/ 下 318 个导出符号逐一 findReferences + grep 交叉验证；`tsc --allowUnreachableCode false` 确认全仓无不可达分支。

**确定未使用（定义外全仓 0 引用，含测试）：**

- `src/components/AnnotationOverlay.tsx`（整个文件 160 行）：已被 `AnnotationCanvas.tsx` 取代，无任何 import。
- `src/lib/utils.ts`（整个文件）：`cn` 全仓 0 引用；**附带：`clsx`、`tailwind-merge` 两个依赖仅此一处使用，可一并移除**。
- `src/utils/annotationCoords.ts:67` `rotatePoint`、`annotations.tsx:25` `getAnnotationTypes`、`chart.ts:12` `isStatisticalChart`、`chart.ts:17` `isMatrixChart`、`colormaps.ts:52` `getColorFromMap`、`contextMenu.ts:36` `useContextMenu`、`sampleData.ts:13` `createEmptyDataset`、`statistics.ts:606` `varianceInflationFactor`、`statistics.ts:685` `jarqueBeraTest`。
- `hypothesisTests.ts:626` 的 `chi2Critical005` 再导出链无人消费。

**疑似"实现未接 UI"的孤儿功能（仅测试引用，删除前需对照 PHASE 文档确认产品意图）：**

- `projectFileV6.ts` 整个模块（117 行）：`projectFile.ts` 已是 `PROJECT_VERSION = 6` 但未 import 此模块——**存在两套 v6 序列化实现**。
- `curveFitting.ts`：`lorentzianFit`、`weibullFit`、`logistic4PLFit`、`logistic5PLFit`、`hillFit`、`biexponentialFit`、`computePredictionBand`、`globalFit`（与 §六.5 拟合 UI 盘点一致）。
- `hypothesisTests.ts`：除 `shapiroWilk` 外全部导出（t 检验、ANOVA、Mann-Whitney、KS 等 11 个）仅测试引用，StatsTab 只做描述统计。
- `statistics.ts`：`stdDev`、`iqr`、`correlation`、`correlationMatrix`、`breuschPaganTest`；`distributions.ts`：`gamma`、`normalPdf`、`chi2Cdf`、`chi2Critical005`；`tracesBuilder.ts:96` `enrichColumn`；`layoutBuilder.ts:304` `buildInsets`；`dataProcessing.ts:448` `filterIndices`、`:574` `removeOutlierIndices`。

### 6.3 historyStore 撤销/重做边界核查（对应 §一"historyStore"项）

**确定 bug：**

1. **（P0）单元格编辑实际不可撤销**。`DataTable.tsx:580-581` 打字时 `updateCellValueSilent` 立即写入 store，失焦时 `updateCellValue`（`datasetStore.ts:233-236`）才 `pushSnapshot`——快照捕获的是**编辑后**状态，undo 恢复到相同状态。AGENTS.md 描述的"失焦提交历史"约定在实现上是坏的，且每次失焦白占一个历史槽位。正确做法：首次 silent 写入前（onFocus）捕获快照。
2. **（P1）查找替换批量填充不进历史**（`DataTable.tsx:174-176`），循环调用 Silent 写入后无任何快照提交。
3. **（P1）`restoreBranch` 后 redo 方向颠倒**（`historyStore.ts:183-189`）：恢复到分支末端却把整个 `entries` 放回 `_future`，按 redo 会跳回分支起点。合理行为：恢复到末端且 `_future` 清空。
4. **（P2）加载项目后未清空 `_branches`**（`FileTab.tsx:569`），恢复残留旧分支会把旧项目 snapshot 覆盖到新项目上。
5. **（P1）ConfigPanel 快照风暴**：标题/轴标签/边距等 `onChange` 直调 setter 逐字符推快照（`ConfigPanel.tsx:220,40,51,187` → `chartStore.ts:149-155`），与 DataTable 的"silent + 失焦提交"约定不一致，50 槽栈很快被无意义条目占满。
6. **（P2）输入框内 Ctrl+Z 被全局劫持**（`Workspace.tsx:204-217`）：`isInputFocused` 已计算却在判断前 preventDefault；且打字中（silent 未提交）按 Ctrl+Z 会冲掉正在输入的内容。
7. **（P2）`restoreBranch` 在 `_future` 为空时仍创建空分支**（`historyStore.ts:176-181`）。

**确认无问题的边界项**：栈满 50 / 分支满 10 的丢弃策略正确无越界；空栈 undo/redo 有守卫且 UI 双层防护；撤销后新操作的分支创建逻辑正确；快照存引用 + 全链路不可变更新 = 结构共享，无 50 倍内存占用（两处就地修改 `chartStore.ts:367`/`datasetStore.ts:500` 均作用在副本上，但该正确性依赖约定，任何未来引入的就地 mutation 会静默腐蚀历史栈）；图表缩放/平移不进历史，无交互快照风暴；undo 后 React 绑定自动同步。

### 6.4 导出路径资源清理核查（对应 §一"导出"项）

11 处 `createObjectURL` 下载点逐一核查：

- **❌ 泄漏：`FileTab.tsx:494-495` CSV 导出从不 `revokeObjectURL`**，每次导出泄漏一个 blob URL 直至页面卸载。
- **⚠️ 竞态风险（低）×4**：`FileTab.tsx:376`（TIFF）、`StatsTab.tsx:150,190`、`MultiPeakFitModal.tsx:168`、`fitExport.ts:100` 均为 `click()` 后**同步立即 revoke**（Safari 等存在下载未开始即被 revoke 的经典问题），且与仓内 1000ms 延迟约定不一致。其余 6 处均为 `setTimeout(revoke, 1000)`，正确。
- **❌ 主要发现：3D 离屏导出从不 `Plotly.purge`**（`exportLayout.ts:237-263`）。`export3DToPng` 的 finally 只 `removeChild(tempDiv)`；3D Plotly 实例持有 WebGL context，浏览器上限约 16 个，超限后最老 context 被强制丢失，可波及屏幕上正在显示的 3D 图表。`exportFigureToPng/Svg`（`exportLayout.ts:582,629`）还 `Promise.all` 并发渲染所有 3D cell，一次导出即并发创建多个未 purge 的 context。
- **TIFF 内存峰值**：`tiffEncoder.ts` 同步未压缩 RGBA 编码，300 DPI + 乘数 3 时缓冲可达 ~170 MB，主线程同步完成会短暂阻塞 UI（性能风险，非泄漏）。
- **无问题项**：所有 `Plotly.toImage` 调用均 await + try/catch，无未处理 rejection；`Plotly.downloadImage` 全仓未调用；fit worker 为模块级单例无 listener 累积（仅存无超时机制与 postMessage 同步异常时 pending 残留两个低概率边缘口）。
- **结论：AGENTS.md §8"延迟后 revokeObjectURL"只部分属实**（11 处中 6 处符合），文档需更新。

### 6.5 §四遗留事项核实结果

1. **EPS 导出**：已确认从所有 UI 入口移除（ExportModal 仅 png/svg/pdf/tiff；ChartView 右键仅 png/svg；FileTab 无 EPS）。唯一残留是 `zh.json:55`/`en.json:55` 的 `exportEps` 孤儿 key。AGENTS.md §9 的"EPS 是改扩展名、标记实验性"描述已过时——功能连同假实现一起被删了。
2. **PDF 导出**：三处 jsPDF 调用全部光栅化。`FileTab.tsx:271-286` 虽走 `addSvgAsImage`，但 jsPDF 内置实现是 canvg 转位图（无 `svg2pdf.js` 依赖），**`:284` 注释 "Embed SVG as a vector image" 是错误的**。AGENTS.md §9"PDF 为 PNG 嵌入"仍然准确。
3. **拟合统计 UI 盘点**（`FitResultsBar.tsx`，挂载于 `Workspace.tsx:436`）：已呈现方程、R²/调整 R²/RMSE、参数表（估计值/标准误/95% CI）、F 检验、残差正态性检验、**残差图**（`ResidualPlotModal`）、导出 CSV/LaTeX、钉为标注。**算法支持但 UI 未呈现**：置信带（`computePredictionBand`）、加权拟合（FitTab 从不传 weights）、参数 bounds、`globalFit`、6 种新 LM 拟合类型（worker `FitType` 与 FitTab 均未包含）。
